Всичко в html-css  е една кутия всяка кутия има няколко чати 

wigth 
heigth

border 
padding 
margin

има 3 типа кутики 

block, inline, inline-block

block - винаги заемат цялата широчина на един елемент --> main header article section fieldset nav ul ol li form h1-h6 p div


inline- заемат толкова място колкото е тяхното съдържание -> a, label, map, span, strong, em i, img, tetarea, input, button, select 

inline-block - може да застане един до друг но той не се пречупва на множество редове 


когато искаме един до друг inline-block Или inline 
а когато сме с едно от край до край  block


Width 
 -> винаги е статиче 
 -> когато използваме % той заема процента в контекста дали е в цялааата страница или е в контейнер
 -> min - max width пак фиксират колко точно да бъде
 -> на inline width не може да се променя винаги размера е неговото съдаржание 

Heiht
 -> също има min - height
 -> когато имаме височина тя няма граница и работи само в контейнер който има конкретна височина



overflow - ако имаме елемент който излиза от контейнера си го задаваме на контейнера 

margin - Пространството извън кутията марждин със всички стойности може да се изпише 


top right botthem left - по часовниковата стрелка 
при две стойности е горе долу, ляво дясно


когато имаме margin в higth и горния има margin-bottem а долния Marin-top  те не се обединяват а се взема по големия

при inline  работи само в хоризонтал 

padding простанството вътре в кутията между съдържанието и бордъра 

top right botthem left - по часовниковата стрелка 
при две стойности е горе долу, ляво дясно

при inline  не работи 

border обрграждащата линия на елемента 
 
border width style (solid, dashed, dotted) color

border radius  заобляне в 4 те ъгъла

box sizing - border-box wid-higt се задават на бордъра а не на контента

В HTML ако елемента ни има спейс или нов ред може да не калкулира правилно размерите можже да се оправи като сложим на контейнера Fout size 0px

